# Copy to .env and adjust. The compose stack reads these variables.

# Password for the baleia_trino role. Both the postgres and trino containers
# read the SAME variable, so they cannot diverge. In production, inject via
# the orchestrator's secret mechanism before `docker compose up`.
BALEIA_TRINO_DB_PASSWORD=change_before_deploy
