# Dev-only: password is hardcoded to 'admin' and matches the role created
# by baleia's migration 00018_trino_catalog_store.sql. For production the
# password line should be replaced with ${ENV:BALEIA_TRINO_DB_PASSWORD}
# and the env injected via Docker secret / k8s secret.
baleia.jdbc-url=jdbc:postgresql://postgres:5432/baleia
baleia.username=baleia_trino
baleia.password=admin
baleia.cluster-name=default
baleia.connect-timeout=10s
baleia.socket-timeout=30s
